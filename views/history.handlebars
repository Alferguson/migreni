<div class="container-fluid">
  <header class="row">
    <h1>Mígreni</h1>
  </header>

      {{!-- history --}}
      <div class="history">
        <br>
        <h2>Your migraine history</h2>
        <table class="table table-striped table-responsive table-sm">
          <thead>
            <tr>
              <th scope="col">#</th>
              <th scope="col">Date</th>
              <th scope="col">Intensity</th>
              <th scope="col">Trigger</th>
              <th scope="col">Chronic Treatment</th>
              <th scope="col">Acute Treatment</th>
              <th scope="col">Temperature</th>
              <th scope="col">Precipitation</th>
              <th scope="col">Humidity</th>
            </tr>
          </thead>
          <tbody>
            {{#each migraines}}
              <tr>
  <th scope="row">{{row_num}}</th>
  <td data="date">{{date}}</td>
  <td data="intensity">{{intensity}}</td>
  <td data="trigger">{{trigger}}</td>
  <td data="chronic_treatment">{{treatment_name}}, {{treatment_dose}} {{treatment_unit}}</td>
  <td data="acute_treatment">{{acute_name}}, {{acute_dose}} {{acute_unit}}</td>
  <td data="temperature">{{temperature}}&deg; F</td>
  <td data="precipitation">{{precipitation}}</td>
  <td data="humidity">{{humidity}}</td>
  <td><button type="button" class="btn btn-dark btn-sm update-migraine" name="{{id}}">Update</button></td>
  <td><button type="button" class="btn btn-dark btn-sm delete-migraine" name="{{id}}">Delete</button></td>
</tr>

            {{/each}}
          </tbody>
        </table>
        <button type="button" class="btn btn-dark btn-lg cancel">Cancel</button>
      </div> {{!-- end history --}}
     
<div class="modal fade" id="migraine-update" tabindex="-1" role="dialog">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h2 class="modal-title" id="title"><strong>Update Migraine</strong></h2>
        <button type="button" class="close close-clear" data-dismiss="modal">&times;</button>
      </div>
      <div class="modal-body">
        <h6></h6>
      </div>
      <div class="modal-footer">
        <button id="submit-update" type="button" class="btn btn-secondary close-clear" data-dismiss="modal">Submit</button>
        <button type="button" class="btn btn-secondary close-clear" data-dismiss="modal">Cancel</button>
      </div>
    </div>
  </div>
</div>
<script src="/assets/js/show-history.js"></script>
{{!-- <script src="/assets/js/migraine.js"></script> --}}